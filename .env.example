# Copy this file to .env (local dev) or set in Vercel project settings.
# VITE_API_BASE points the frontend at the deployed FastAPI backend.
# When unset, defaults to http://localhost:8000 (see src/lib/api.js).

VITE_API_BASE=https://gp-restaurant-api.onrender.com
